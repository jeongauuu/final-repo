apiVersion: v1
kind: Service
metadata:
  name: final-nodejs-service
  namespace: final-space
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: final-nodejs-app # Deployment의 label과 일치해야 함